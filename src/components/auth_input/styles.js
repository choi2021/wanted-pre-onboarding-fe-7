import styled from 'styled-components';

const AuthInput = styled.input`
  width: 15rem;
  height: 2rem;
  border-radius: 1em;
  margin-bottom: 1em;
  outline: none;
  text-align: center;
`;

const S = { AuthInput };
export default S;
