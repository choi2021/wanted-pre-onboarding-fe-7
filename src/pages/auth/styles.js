import styled from 'styled-components';

const AuthLayout = styled.section`
  max-width: 40rem;
  margin: auto;
  height: 100%;
  background-color: lightseagreen;
  color: white;
  display: flex;
  flex-direction: column;
`;

const S = {};
S.AuthLayout = AuthLayout;

export default S;
